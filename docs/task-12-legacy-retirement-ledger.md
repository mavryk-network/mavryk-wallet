# Task 12: legacy retirement ledger and ready-to-file ticket

## Status and filing

Inventory prepared on 2026-09-10. Task 12 is **incomplete pending an agreed deletion-marker version**. No release version is inferred from package.json (2.0.8), tags, elapsed time, or a local merge. No deletion is authorized by this ledger or a future marker.

Ready-to-file ticket title: **Retire legacy migration bridges after verified shipped migrations and supported upgrade paths (Task 21 / Phase 9)**.

External issue URL: none. Authenticated `gh repo view --json nameWithOwner,hasIssuesEnabled` returned `mavryk-network/mavryk-wallet` with `hasIssuesEnabled: false`; `gh issue list --state all` returned “repository has disabled issues”. Repository history uses MAV identifiers and source mentions Jira, but inspected tracked Markdown and workflow files provide no Jira project URL or accessible destination. No tracker connector is available. Do not enable GitHub issues or invent a Jira destination. This document is the ready-to-file ticket body and canonical inventory; file it in the established tracker once its destination/access is available, first searching for an equivalent ticket, then record its URL here. External duplicates cannot currently be ruled out in Jira. No external issue was created.

No existing Task 12 ledger or branch was found; GitHub PR search for `task-12-redem` returned no results. GitHub release listing was empty and local tags establish no future retirement decision. Tasks 10–11 are reused unchanged. The ignored local Task 12 and Task 21 descriptions are preserved; this ledger corrects their reference-only assumptions about dead callers and retired storage. Historical plans remain historical.

## Starting evidence

Fetch of origin and explicit PR #30 head succeeded on 2026-09-10. Starting tree was clean.

| Ref                                           | SHA                                                                                     |
| --------------------------------------------- | --------------------------------------------------------------------------------------- |
| HEAD / local MAV-3974/architecture            | `1abf90f7c35778303b362290883144a707119295`                                              |
| origin/MAV-3974/architecture                  | `9789f42c435181273f039ca595b0bdea1fb69431`                                              |
| origin/dev                                    | `1cc65ac19a7e3c931def7fc5071d6697ecaee0a9`                                              |
| origin/pr/30 / origin/dev-architecture-update | `523a21a348fcbef3330f64aa2da1c00fecb45a7e`                                              |
| Local dev-architecture-update                 | `50c10e72478c36f5acb0a87b6e1bf8c7efc0fc7c`                                              |
| Task 11 implementation / local merge          | `8c4dc9168e5127306cbcd1193d5275b2bf931f10` / `dc8f3f7ff6e4babdc2ce9993c2e7d0e693a38f0a` |
| Task 10 implementation                        | `8287461aebd154fe470e227f3f671236a62722ff`                                              |

The supplied baseline differs only by the subsequent AGENTS/.gitignore commit, retained intact. New branch `task-12-redem` starts at current integration. PR #30 is reference only; dev remains the production lineage/final target. No push or merge into dev. Commit and local integration outcome belong in the delivery report.

## Bridge inventory and marker boundaries

Paths below are relative to the repository root. A future concrete `DELETE IN` comment must link to this ledger and apply only to the stated scope. No whole-module deletion is justified merely by a legacy filename.

| Path                                           | Temporary scope and dependencies                                                                                                                                                                                                                                                           | Protections / live behavior to retain                                                                                                                                                                                                                                                                                                                 |
| ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/lib/store/zustand/legacy-ui-migration.ts` | Legacy source read, preference/root metadata adoption and completion-writing path inside `createLegacyUIMigration`; remove only after supported old profiles have a replacement upgrade path or are explicitly excluded by policy.                                                         | Mixed-purpose module: hydration/retry, durable UI/metadata/identity consistency checks, fresh identity initialization and readiness are used by the owner even after migration. Extract/preserve equivalent startup behavior before removing the factory.                                                                                             |
| `src/lib/store/zustand/legacy-ui-source.ts`    | `LEGACY_UI_ROOT_KEY`, legacy decoding and field mapping in `decodeLegacyUIRoot` / `parseLegacyUIRoot`. Depends on migration, foreground fallback and Redux root decoding callers.                                                                                                          | `ANALYTICS_ID_SCHEMA` is current identity validation; preserve/relocate its contract. `decodeLegacyUIRoot` also decodes the active task11 Redux root on every load, so it cannot disappear merely when old-root fallback ends.                                                                                                                        |
| `src/lib/store/zustand/ui-owner.ts`            | Fallback cache, `readFallback`, fallback request consumption/reset and `fallback-required` response handling; migration invocation only after a replacement readiness initializer exists.                                                                                                  | Permanent background authority, sender checks, queue, command validation, flush/read-back, retry, write-failure suppression and consent/identity readiness. Never mark the whole module disposable.                                                                                                                                                   |
| `src/lib/store/zustand/ui-client.ts`           | Only the `fallback-required` branch in `send` and its legacy-key import.                                                                                                                                                                                                                   | Permanent read-only snapshots, serialized commands, storage-change subscription and error/readiness gate.                                                                                                                                                                                                                                             |
| `src/lib/store/zustand/ui-owner.contract.ts`   | Only `UI_REQUEST_SCHEMA.fallback`, coordinated with both protocol endpoints.                                                                                                                                                                                                               | Keep strict request/command/snapshot schemas, identity/completion write exclusion and channel contract.                                                                                                                                                                                                                                               |
| `src/app/store/owned-ui.middleware.ts`         | Compatibility action-to-command adapter; retirement requires every intercepted action producer to be converted and metadata transformations/loading reset behavior preserved.                                                                                                              | Currently prevents old Redux reducers from competing with the owner. It must stay until caller handoff; removal is not implied by storage migration completion.                                                                                                                                                                                       |
| `src/app/store/index.ts`                       | Only the nullish fallback read of `key: 'temple-root'` in `getStoredState`.                                                                                                                                                                                                                | `temple-root-task11` persistence, current-root decoding, unrelated Redux domains, reducers/epics and manual persistence startup remain live.                                                                                                                                                                                                          |
| `src/lib/store/persist.utils.ts`               | Only the serialized localStorage fallback in `getStoredStateToMigrateStorage`. Inventory every `storageConfig` consumer before removal, including roots outside the seven historical keys.                                                                                                 | Browser read, transforms and persistence configuration remain live infrastructure.                                                                                                                                                                                                                                                                    |
| `src/app/hooks/use-user-id-sync.ts`            | No migration/fallback writes remain. No deletion marker warranted.                                                                                                                                                                                                                         | Readiness selector only; identity adoption belongs to the background. Do not restore the old identity writer.                                                                                                                                                                                                                                         |
| `src/intercom-client.ts`                       | Not a confirmed temporary bridge or dead module. No deletion marker.                                                                                                                                                                                                                       | `src/contentScript.ts` imports `getIntercom` and calls it at lines 103, 141 and 173 (dApp request/subscription paths). Module import also constructs the exported client. Repository import search proves a live consumer; recheck symbols and side effects before any future separately scoped removal. Preserve Task 8 timeout/disconnect behavior. |
| Task 13 modules                                | Pending inventory after implementation; no new paths or completion keys asserted.                                                                                                                                                                                                          | Must transfer nested assets/statuses, adult flags, promotion preferences and collectible/RWA metadata through coordinated durable ownership.                                                                                                                                                                                                          |
| Task 14 modules                                | Pending inventory after implementation. Existing `src/app/hooks/use-assets-migrations.ts` registers `assets-migrations@1.18.2` and `src/lib/assets/migrations.ts` still targets Redux and clears `Repo.accountTokens`. These are live prior implementation, not completed Task 14 bridges. | Do not change registration or IndexedDB behavior in Task 12. Task 14 must establish its actual fallback/classification, durability and completion contracts before extending this ledger.                                                                                                                                                             |

Shared `await-stores-hydrated.ts`, `destination-store.ts`, `persist-storage.ts`, `throttled-storage.ts`, `validation.ts`, destination schemas/stores and analytics gates under `src/lib/analytics/` and `src/lib/temple/back/analytics.ts` are protections, not disposable migration modules. Names in this sentence refer to `src/lib/store/zustand/` unless fully qualified.

## Storage inventory

Browser persistence uses `src/lib/store/storage.ts` through `storageConfig` in `src/lib/store/persist.utils.ts`; Redux stores plain objects (`serialize: false`, `deserialize: false`). The fallback uses redux-persist localStorage serialization. All nested reducers below are registered in `src/app/store/root-state.reducer.ts`.

| Exact key                                               | Current reader/writer and disposition                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `persist:temple-root`                                   | Preserved Task 11 source/rollback snapshot. Read by UI migration and old-root fallback in `src/app/store/index.ts`; current Redux writes use the new root. Retain while either reader or supported upgrades depend on it; root fields beyond Task 11 still matter.                                                                                                                                                 |
| `persist:temple-root-task11`                            | **Active unrelated Redux root**, read/written by `src/app/store/index.ts`. Retains balances, currency/rates, swap, notifications/statuses, active advertising and other non-blacklisted data; partner promotion is also not root-blacklisted. Not an obsolete cleanup key. Requires a separate complete owner handoff before any future retirement proposal.                                                       |
| `persist:root.assets`                                   | Live `src/app/store/assets/reducer.ts`: token/collectible/RWA records and statuses/manual flags, whitelist/scamlist state. Task 13 data source; Redux remains owner.                                                                                                                                                                                                                                               |
| `persist:root.collectibles`                             | Live `src/app/store/collectibles/reducer.ts`: whitelist persists `adultFlags`. Task 13 source; Redux owner.                                                                                                                                                                                                                                                                                                        |
| `persist:root.rwas`                                     | Live `src/app/store/rwas/reducer.ts`: whitelist persists `adultFlags`. Task 13 source; Redux owner.                                                                                                                                                                                                                                                                                                                |
| `persist:root.collectiblesMetadata`                     | Live `src/app/store/collectibles-metadata/reducer.ts`: records transformed Map↔array, `isLoading` excluded. Task 13 source; Redux owner.                                                                                                                                                                                                                                                                           |
| `persist:root.rwasMetadata`                             | Live `src/app/store/rwas-metadata/reducer.ts`: records transformed Map↔array, `isLoading` excluded. Task 13 source; Redux owner.                                                                                                                                                                                                                                                                                   |
| `persist:root.partnersPromotion`                        | Live `src/app/store/partners-promotion/reducers.ts`: whole slice including `shouldShowPromotion`, `promotionHidingTimestamps` and fetched promotion state. Task 13 source; Redux owner.                                                                                                                                                                                                                            |
| Serialized localStorage copies of all eight roots above | Readable through `getStoredStateToMigrateStorage`; old UI root additionally requested explicitly by the owner through `ui-client.ts` only after an absent browser source. Retained fallback data, never globally cleared. The task11 copy is a supported fallback address, not evidence that a historical writer actually created it. Inventory other storageConfig consumers before retiring the shared fallback. |
| `analytics_user_id`                                     | Preserve. Background UI migration adopts/writes and independently verifies identity using `ANALYTICS_USER_ID_STORAGE_KEY` in `src/lib/constants.ts`; owner controls ready identity/consent.                                                                                                                                                                                                                        |
| `zustand-ui`                                            | Preserve. `src/lib/store/zustand/ui.store.ts`, single background UI owner for activated preferences/identity; includes active `legacyMigrated` completion state. Promotion destinations remain inactive pending Task 13.                                                                                                                                                                                           |
| `zustand-metadata`                                      | Preserve. `src/lib/store/zustand/metadata.store.ts`, background owner for root token metadata; existing nested destination data retained but ownership not activated.                                                                                                                                                                                                                                              |
| `zustand-assets`                                        | Preserve. `src/lib/store/zustand/assets.store.ts`, Task 10 destination infrastructure, no production asset writer activation. Redux remains authoritative pending Task 13.                                                                                                                                                                                                                                         |
| Other migration-completion state and unrelated storage  | Preserve, including localStorage `MIGRATIONS` history (`src/lib/local-storage/migrator.ts`) and vault migrations. No wildcard/prefix clearing, IndexedDB deletion or storage-prefix rename.                                                                                                                                                                                                                        |

`legacyMigrated` is a field in the UI envelope, not a standalone key and not proof that Tasks 13/14 ran. Redux/SWR/Effector search hits remain live-domain inventory, never a dead-code deletion list.

## Future retirement acceptance criteria

- [ ] Record the actual release versions/dates that ship each required migration and reader/writer handoff. Require at least one clean release cycle after those releases; a local merge is not a shipped release.
- [ ] Complete Tasks 13 and 14 wherever their source data is involved, and replace their pending inventory above with actual modules, keys, owners and completion semantics.
- [ ] Approve a supported-upgrade-path policy for users skipping releases. Either keep a compatibility migration for every supported direct upgrade or specify/enforce a supported intermediate upgrade/recovery route. Elapsed time and aggregate migration counts alone cannot prove every profile migrated. Do not silently classify unsupported or partially migrated profiles as fresh installs.
- [ ] For each proposed code/key deletion, prove zero remaining live readers/writers, including older concurrently open contexts, rollback versions, fallback hydration and startup registrations. Re-run scoped import/symbol/key searches at the retirement commit.
- [ ] Verify durable destination data and completion state with actual adapter flush/read-back. Cover failed writes/reads, retry, interrupted startup, partial cross-store persistence, concurrent foreground/background contexts and two reloads. Do not delete sources to compensate for a partial migration.
- [ ] Preserve analytics ID/consent and readiness, every custom asset/status/manual flag, account/network scoping, metadata, adult flags, promotion preferences and unrelated domains. Fresh-install identity creation must still work once legacy adoption code is removed.
- [ ] Test fresh, already migrated, supported old direct upgrades, skipped releases, malformed sources, absent browser source with serialized fallback, and interrupted/failed migrations. Add explicit allowlisted cleanup tests that assert protected keys and IndexedDB remain untouched outside separately approved scope.
- [ ] Document rollback implications: retained old snapshots may be stale after new-owner writes; define rollback data/recovery behavior before deleting anything. Maintain rollback sources for the required clean release cycle and longer where the supported-upgrade policy requires them.
- [ ] Replace migration-coupled readiness with equivalent permanent hydration, validation, single-owner serialization, durable verification and analytics gates before removing obsolete fallback code. Migrate Redux action callers before removing middleware.
- [ ] Review the concrete deletion-marker version against release evidence. A reached version is a review reminder, never an automatic cleanup trigger. Defer retirement if any prerequisite fails.

Still-needed decisions: agreed marker version, release evidence, supported minimum/direct upgrade policy and rollback recovery policy, and external tracker destination/access. No release or upgrade-policy approval is invented here.

## Verification and Task 13 handoff

Inventory is matched to the current Task 11 implementation and live reducer registrations. No executable changes are needed. Markdown formatting, all 29 explicit source paths, relative documentation links and `git diff --check` were verified; runtime tests/builds do not validate documentation-only edits and are intentionally not rerun. Task 11's recorded baseline remains in [its handoff](task-11-legacy-ui-handoff.md); no new runtime pass or installed-profile smoke test is claimed.

Task 13 can use the unchanged [Task 10 foundation](task-10-zustand-foundation.md) and [Task 11 owner contract](task-11-legacy-ui-handoff.md). Task 12 cannot be called complete until concrete scoped markers are added after the version decision. Task 13 must extend this inventory as it establishes actual ownership; this ticket does not authorize its implementation.

## Documentation Update

Added this tracked ledger as the ready-to-file future retirement ticket, correcting reference assumptions without replacing ignored task descriptions. No runtime behavior, API, dependency, persistence registration, source payload or IndexedDB change is included.
